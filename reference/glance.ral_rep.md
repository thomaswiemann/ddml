# Glance at a RAL Rep Object

Glance at a RAL Rep Object

## Usage

``` r
# S3 method for class 'ral_rep'
glance(x, ...)
```

## Arguments

- x:

  An object inheriting from class `ral_rep`.

- ...:

  Currently unused.

## Value

A one-row `data.frame` with columns `nobs`, `nresamples`, and
`estimator_name`.
